# --- MQTT / Home Assistant ---
MQTT_HOST=192.168.178.***
MQTT_PORT=****
MQTT_USER=mqtt
MQTT_PASS=********
MQTT_BASE_TOPIC=nilm
MQTT_HA_DISCOVERY=true
MQTT_HA_PREFIX=homeassistant
MQTT_RETAIN=false
MQTT_QOS=0

# --- Modell / Artefakte ---
MODEL_ARTIFACT_DIR=/home/falkh/hems-nilm-gateway/artifacts/mgru_ofat_s2s/ID_2025-11-13_193225_26
MODEL_DEVICE_IDS=24,26,35
MODEL_DEVICE_NAMES="Washing Machine,Dish Washer,Refrigerator"

# --- Streaming / Feature-Fenster ---
STREAM_WINDOW=960
STREAM_STRIDE=10
STREAM_SAMPLE_RATE_HZ=1.0
STREAM_REPLAY_SPEED=10.0
STREAM_USE_DEDDIAG=false

# --- DEDDIAG Datenbank ---
DEDDIAG_SCHEMA=public
DEDDIAG_MAINS_ITEM_ID=59
DEDDIAG_START=2017-11-23T12:00:00
DEDDIAG_END=2017-12-08T12:00:00
DEDDIAG_DB_HOST=192.168.178.20
DEDDIAG_DB_PORT=5432
DEDDIAG_DB_NAME=postgres
DEDDIAG_DB_USER=postgres
DEDDIAG_DB_PASSWORD=********

# --- Shelly 3EM (Live-Messung) ---
# Wird verwendet, wenn STREAM_USE_DEDDIAG=false
SHELLY_HOST=192.168.178.***
SHELLY_PORT=****
SHELLY_TIMEOUT_S=3.0

# --- Runtime / NILM-Postprocessing ---
GROUNDTRUTH_ON_W=10.0
EMA_ALPHA=1.0

# --- Host-Metriken ---
PUBLISH_PI_METRICS=true
PI_METRICS_INTERVAL_S=5
